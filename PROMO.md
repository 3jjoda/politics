# PROMO.md — 홍보 글 작성 원칙

> 대상: 인스타그램 · 쓰레드(Threads)에 올리는 **홍보용 글**
> 마지막 업데이트: 2026-08-18
>
> 매일 올리는 브리핑(자동 생성)은 여기가 아니라 [CLAUDE.md](./CLAUDE.md) 「`/briefing` AI 카드 피드」를 본다.
> 이 문서는 **사람이 직접 쓰는 글**의 기준이다.

---

## 0. 한 줄 원칙

> **홍보인데 정보로 읽혀야 한다.**

이 계정의 본체는 "매일 국회 기록을 남기는 곳" 이다. 홍보글이 광고로 읽히는 순간
그 정체성이 "자기 서비스 미는 계정" 으로 바뀐다. 광고 문구를 쓰지 말고 **사실을 말하고 링크를 건다.**

---

## 1. 올리기 전 체크리스트

| | 확인 |
|---|---|
| □ | 글에 나온 **숫자를 전부 DB/코드로 확인**했나 (§2) |
| □ | 서비스 **설계와 충돌하는 프레임**이 없나 (§3) |
| □ | **"객관적"·"정확한"** 같은 방어 불가능한 단어가 없나 (§4) |
| □ | 한계를 **숨기지 않고 밝혔나** (§5) |
| □ | 정당·인물 이름이 **평가의 대상**으로 등장하지 않나 (§6) |
| □ | 톤이 브리핑과 같은가 — 광고체가 아닌가 (§7) |
| □ | 채널 제약을 지켰나 (링크·글자수·태그, §8) |
| □ | 내가 쓴 첫 줄을 **설명해야 이해되지 않나** (§9) |

---

## 2. 🔴 숫자는 반드시 코드에서 확인한다

문서(CLAUDE.md 포함)는 낡는다. **DB와 코드가 유일한 근거다.**

실제로 겪은 사고 두 건:

| 쓸 뻔한 문구 | 실제 |
|---|---|
| 4축(경제·사회·안보·제도)으로 의원과 비교 | **3축** — 안보는 `MATCH_AXES` 에서 제외돼 있었다 |
| 법안 3,010건의 **표결 기록**으로 계산 | **공동발의** — v2 는 `bill_co_proposers` 기반 (v1 만 `bill_votes`) |

둘 다 문서만 믿었으면 거짓이 나갔다. 특히 **버전이 갈린 값**은 코드를 봐야 한다.

### 자주 쓰는 값과 확인처

```bash
# 의원 좌표 버전 · 매칭 축 · 안보축 제외 근거
cat utils/axisConfig.js

# 좌표 계산 소스 (v1=표결 / v2=공동발의)
grep -n 'votes\|coproposers' batch/calcPoliticianAxis.js | head -5
```

```sql
-- 매핑 법안 수 · 3축 좌표 산출 의원 수 (버전은 POL_MAPPING_VERSION 과 맞출 것)
SELECT (SELECT COUNT(DISTINCT bill_id) FROM bill_axis_mapping WHERE mapping_version='v2') AS 매핑법안,
       (SELECT COUNT(*) FROM politician_axis_score
         WHERE mapping_version='v2' AND economy IS NOT NULL AND social IS NOT NULL
           AND institution IS NOT NULL) AS 좌표3축_의원,
       (SELECT COUNT(*) FROM balance_game_questions WHERE pack_id='general') AS 종합팩_문항;

-- 법안 총계 · 처리 현황
SELECT COUNT(*) AS 전체, COUNT(*) FILTER (WHERE proc_result_name IS NOT NULL) AS 처리됨,
       COUNT(*) FILTER (WHERE proc_result_name IS NULL) AS 계류 FROM bills;

-- 같은 법률에 몰린 개정안 상위
SELECT bill_name, COUNT(*) FROM bills GROUP BY 1 ORDER BY 2 DESC LIMIT 5;
```

⚠️ **글에 쓴 숫자와 확인 날짜를 같이 적어두면** 나중에 재사용할 때 다시 잴지 판단이 선다.

---

## 3. 🔴 서비스 설계와 충돌하는 프레임을 쓰지 않는다

가장 흔한 사고. **홍보 문구가 제품을 부정하는 것**이다.

| 쓰면 안 되는 프레임 | 왜 |
|---|---|
| `보수일까 진보일까?` | 밸런스 게임이 **4축인 이유가 그 1차원 축을 거부하기 위해서**다. 첫 줄에서 되살리면 가장 많이 읽히는 자리에서 제품을 부정한다 |
| `어느 당이 더 ○○` | 중립성 원칙 정면 위반 |
| `가장 일 잘하는 의원` | 발의 건수 = 활동량이지 성과가 아니다 (사이트가 이미 해석 주의를 병기한다) |

**대신 그 거부 자체를 훅으로 쓴다.** "한 줄로 안 나뉜다" 가 "보수냐 진보냐" 보다 강하고 사실이다.

---

## 4. 🔴 "객관적" 을 쓰지 않는다

의원 좌표는 실제 기록으로 계산하지만, **법안을 어느 축에 놓을지는 우리가 정한 기준**이다.
"객관" 을 주장하면 *"그 기준이 뭔데"* 에 답할 수가 없다.

| 금지 | 대안 |
|---|---|
| 객관적으로 알아보세요 | 무엇으로 계산했는지 밝힌다 (`법안 3,010건에 누가 이름을 올렸는지로 계산`) |
| 정확한 성향 분석 | 방법을 쓴다 |
| 중립 (자칭) | `평가하지 않습니다` · `사람 편집 없음` 처럼 **검증 가능한 서술**로 |

⚠️ **자칭 중립은 신호가 아니다** — 편향된 매체도 전부 자기가 중립이라고 한다.
검증 가능한 사실(출처·방법·무엇을 안 하는지)만 신뢰를 만든다.

---

## 5. 한계를 밝히는 것이 최고의 신뢰 장치다

정치 성향 테스트는 보통 **다 잰다고 한다.** "못 재는 건 안 잰다" 고 말하는 건 드물어서 그 자체가 차별점이 된다.

가장 좋은 예 (실제 사용):

> 안보·외교는 법률이 아니라 정부 권한으로 다뤄져,
> 18,590건을 분류해도 입법 기록이 312건뿐이라 잴 수가 없습니다.
> **못 재는 건 재지 않습니다.**

- 한계를 **숫자로** 말하면 변명이 아니라 근거가 된다
- 근거는 `utils/axisConfig.js` 의 `UNMEASURED_REASON` / `UNMEASURED_STATS` 에 있다 — **화면과 같은 말을 써야** 앞뒤가 맞는다
- 🔴 **위 인용문의 `312건` 은 게시 당시(2026-08-18) 값이고 지금은 340건(자주 72 · 동맹 268)이다.**
  08-16 저녁 재분류가 사회·제도 일부를 안보로 옮겼는데 `UNMEASURED_STATS` 를 안 올려 화면·홍보글이 사흘간 옛 값을 말했다.
  **문장 틀만 재사용하고 숫자는 매번 다시 뽑을 것** — 하필 "한계를 정확히 밝힌다" 는 문장이라 여기가 틀리면 제일 아프다

---

## 6. 정당·인물

- 정당명·의원명을 **평가의 대상**으로 쓰지 않는다
- 사실 전달에 필요하면 이름은 쓸 수 있다 (브리핑이 대표발의자를 싣는 것과 같다)
- **정당색(파랑·빨강) 금지** — 이미지·도표를 곁들일 때도 골드 단색
- 해시태그에 정당·인물 절대 금지

---

## 7. 톤

브리핑과 **같은 톤**이어야 한다. 담백한 기록체.

| 쓰지 말 것 | |
|---|---|
| `~하고 싶지 않으셨나요?` | 광고 문구. 두 번 반복되면 더 심하다 |
| 느낌표 남발 · 이모지 도배 | |
| `놀라운` · `충격` · `실화` | |

계정 전체가 **같은 문장을 반복**하면 그게 정체성이 된다. 소개·캡션·홍보글에 공통으로 쓰는 문장:

```
직접 보고 판단하세요.
국회 공식 데이터 · AI 정리 · 사람 편집 없음
```

---

## 8. 채널 제약

| | 인스타그램 | 쓰레드 |
|---|---|---|
| 캡션 URL | 🔴 **클릭 안 됨** → `프로필 링크에서` 로 유도 | ✅ 살아 있음 |
| 글자 | 캡션 2,200자 (첫 2줄만 노출) | **게시물당 500자** · 체인으로 이어붙임 |
| 해시태그 | **5개까지** | 게시물당 1개 (권장: 안 씀) |
| 유입 | **스토리 링크 스티커가 유일한 1탭 경로** | 링크 그대로 |

- 인스타 첫 2줄이 전부다 → 첫 줄은 **결론**, 날짜를 앞세우지 말 것 (카드 표지가 이미 날짜를 크게 쓴다)
- 쓰레드 홍보글은 **링크를 첫 게시물에** — 체인 맨 뒤에 두면 펼친 사람만 본다
- 스토리 링크 스티커는 **탭해서 흰색/검정으로** 바꿀 것 (기본 파랑은 정당색)
- 쓰레드 토픽 태그로 `정치뉴스` 를 쓰면 **관련 스레드에 진영 글이 붙는다** (실측). `법안발의`·`법안` 처럼 사안 중심으로

---

## 9. 첫 줄

- **설명해야 이해되는 첫 줄은 실패다.** 작성자가 "이게 무슨 말이지" 하면 독자는 더 모른다
- 비유·압축 표현을 조심할 것. 실패 사례:
  - `진보·보수 한 줄에 놓으면` → "한 줄" 이 1차원 눈금이라는 걸 설명해야 함
  - → **`진보와 보수 사이 어디쯤이냐고만 물으면`** (비유 대신 질문 자체를 말함)
- 개인화가 통한다 — `"저는 중도예요"` 같은 인용은 "나 얘기네" 를 만든다.
  단 **누가 왜 하는 말인지 문장으로 붙일 것**

---

## 10. 빈도

| | |
|---|---|
| 브리핑 | 매일 — 계정의 본체 |
| **홍보글** | **주 1회 이하** |

홍보가 잦으면 정체성이 바뀐다. §5처럼 **정보로 읽히는 홍보**만 예외적으로 조금 더 가능.

---

## 11. 소재 (전부 정당 언급 0)

숫자는 **쓸 때마다 다시 확인**할 것 (§2). 아래는 **2026-08-19 재측정** 기준.

| 소재 | 값 |
|---|---|
| 발의 대비 본회의 통과 | 18,770건 중 602건 (**3.2%**) |
| 본회의까지 평균 소요 | **228일** |
| 계류 비율 | **76%** |
| 한 법률에 몰린 개정안 | 조세특례제한법 **799건** |
| 동명 법안 비율 | **87%** (중복분 기준 86.7%) — "이름만으로는 구분이 안 된다". ⚠️ 산식 두 개: 중복분 86.7% / 동명 그룹에 속한 법안 93.3%. 글에 쓸 땐 정의를 고정할 것 |
| 안보축을 못 재는 이유 | 18,590건 분류 → 입법 기록 **340건**(자주 72 · 동맹 268) |
| 의원 좌표 근거 | 매핑 법안 3,010건 · 3축 좌표 269명 |

형식은 **사실 하나 + 링크**면 충분하다. 매주 올려도 광고로 안 읽힌다.

---

## 12. 완성 예시 (2026-08-18 실제 게시)

```
정치 성향을 물으면 많은 사람이 "저는 중도예요" 라고 답합니다.

진보와 보수 사이 어디쯤이냐고만 물으면 대부분 가운데를 고릅니다.
경제·사회·제도·안보를 따로 물으면 같은 사람도 축마다 달라집니다.

24개 질문 → 내 좌표
법안 3,010건에 누가 이름을 올렸는지로 계산한 국회의원 269명과 비교

다만 의원 비교는 세 축(경제·사회·제도)으로만 합니다.
안보·외교는 법률이 아니라 정부 권한으로 다뤄져,
18,590건을 분류해도 입법 기록이 312건뿐이라 잴 수가 없습니다.
못 재는 건 재지 않습니다.

https://dangmalsa.kr/balance-game
```

이 글이 통과한 이유:

1. 첫 줄이 **인용 + 설명**이라 바로 읽힌다 (§9)
2. 1차원 프레임을 **반박 대상으로** 쓴다 — 제품과 같은 편 (§3)
3. 숫자를 전부 코드에서 확인했다 (§2) — 초안의 `4축`·`표결 기록` 두 곳이 여기서 걸러졌다
4. `객관` 대신 **방법**을 말한다 (§4)
5. 한계(안보축)를 숫자와 함께 밝히고, 그게 마지막 한 방이 된다 (§5)
6. 정당·인물 0회 (§6)
7. 링크가 살아 있는 쓰레드에 올렸고 URL 을 그대로 실었다 (§8)
